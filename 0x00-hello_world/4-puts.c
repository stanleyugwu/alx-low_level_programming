/**
 * Displays a string in the console
 */

#include <stdio.h>

 /**
  * main - Puts a string to stdout
  *
  * This function puts a string to the standard output (stdout)
  * using the puts function. It prints the message:
  * "Programming is like building a multilingual puzzle"
  *
  * Return: Returns exit code 0
  */
 int main(void)
{
       	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
