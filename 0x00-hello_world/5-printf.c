/* Prints text to console */


#include <stdio.h>

/**
 * main - Main entry point of program
 *
 * Uses printf to write to stdout
 *
 * @return: returns exit code 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
